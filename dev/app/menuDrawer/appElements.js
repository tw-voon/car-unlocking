angular
      .module('vowolita.menuDrawer.elements',[])
      .factory('appElements',appElements);

function appElements () {
  var element = {
    generate:element
  }
  return element;

  function element() {
    var appElements={};

    return appElements;

  }
}
